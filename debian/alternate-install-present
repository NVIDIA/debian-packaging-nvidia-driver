The package that is already installed is named nvidia-driver.

You can upgrade the driver by running:
`apt-get install nvidia-driver`

You can remove nvidia-driver, and all related packages, by running:
`apt-get remove --purge nvidia-driver`
`apt-get autoremove`

This package is maintained by NVIDIA (cudatools@nvidia.com).
