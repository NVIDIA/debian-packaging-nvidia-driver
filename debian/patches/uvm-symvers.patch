Description: Fix unresolved nvUvm* symbols in nvidia-uvm.ko from dkms package
 This patch is applied after unpacking the source tarball, rather than applied
 directly to the module's source tree (i.e. debian/module/debian/patches)
 because nvidia-uvm is only available on amd64 and applying the patch directly
 to the module/dkms package (as suggested by bug reporter in #789483) would
 cause a FTBFS on !amd64 (or additional convoluted calls to quilt in d/rules).
Author: Vivek Das Mohapatra <vivek@collabora.com>
Bug-Debian: https://bugs.debian.org/789483
Last-Update: 2015-07-21

--- a/NVIDIA-Linux-amd64/kernel/uvm/Makefile
+++ b/NVIDIA-Linux-amd64/kernel/uvm/Makefile
@@ -172,6 +172,8 @@
 RM_MODULE_SYMVERS:= $(RM_OUT_DIR)/Module.symvers
 UVM_MODULE_SYMVERS:= $(obj)/Module.symvers
 
+KBUILD_EXTRA_SYMBOLS += $(RM_MODULE_SYMVERS)
+
 module $(MODULE_NAME).ko: $(UVM_MODULE_SYMVERS) debug_diagnostics_printing
 
 $(MODULE_NAME)-y := $(MODULE_GLUE_OBJS)
